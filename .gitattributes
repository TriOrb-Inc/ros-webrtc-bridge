# Preserve original bytes of hash-pinned upstream compiler output, separately from whitespace checks on our own files.
vendor/werift-datachannel/prepared-core/** whitespace=-blank-at-eol,-blank-at-eof
